# A07 实施契约

2026-09-17，revision 1；任务 A07-IMPLEMENT-20260917；风险 L0，虚构教学。

基线：7e9f1bf（A06），开工工作区干净。Project-to-Act 只读发现为 unconfigured；按用户计划沿用 docs/aXX_results.md，不初始化平行账本。正式生命周期 revision 不适用。本任务从功能开发进入离线验证；真实检索、真实回答与个人独立学习分别验收。

范围：app/、data/lore/、tests/test_a07*.py、scripts/a07*.py、exercises/a07*.py、docs/a07*、README.md、agent.md。不修改世界裁定规则、旧课程原始测试证据；仅给 A05/A06 结果文档追加已提交状态补记，不引入框架或数据库服务，不运行未明确启用的真实服务。

不变量：设定不等于经历或账本；角色和接收者交集先于评分；只读检索不提交世界事件；lore_refs 仅引用本轮实际装入请求的片段；A06 source_refs 仍只接受事件；隐藏正文及元数据不进入普通输出；快照不热更新。

固定上限：12 份种子、24 块；400 Unicode 字符/块；query 1—200 字符、top_k 1—3；正文最多 1200 字符/工具结果。4 步/角色、2 个工具/批、场景 30 秒/8 次外部尝试，其中在线 Embedding 最多 2 次；每故事默认 24 次，进程新局不重置。索引显式构建：最多 24 条文本、8 次外部尝试/30 秒。messages+tools 8000 字符，完整片段及完整工具协议块裁剪。

执行顺序：加载/切块/授权 → 关键词与 Fake 场景纵向路径 → 向量/缓存/真实适配 → 故障与镜像测试 → SQL/源码参考 → 全量回归及三层报告。当前用户要求实施，取代所附历史规划任务“只写计划”的范围。

上下文：用户 A07 计划、agent.md、README.md、docs/a06_results.md、当前预算/工具/记忆/场景接口。验收矩阵和证据归入 a07_results.md；命令、退出码、工件哈希由 a07_evidence_manifest.json 记录。

学习者原预测尚未收到；已发出独立预测问题。AI 参考实现不计个人成绩。A05/A06 的真实与独立学习欠项继续保留。
