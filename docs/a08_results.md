# A08 实施结果与下一步

2026-09-17；任务 A08-IMPLEMENT-20260917，revision 2，L0 本地虚构教学。
**A08 离线工程已实现并验证；真实混合/重排质量、真实约 20 轮试玩与本人独立验收仍待完成。阶段 B 尚未通过，不进入 A09。**

事实源沿用本目录课程结果；没有新增 Project-to-Act 平行账本，生命周期 revision 不适用。
基线 HEAD 为 eca50dd4a0e1e026282af7a169d03891e016aff8；当前是其上的未提交 A08 与并行 A07 收尾改动。
开工基线 276 项通过；并行 A07 任务另新增 2 项，本轮将两项一并回归，没有覆盖其修复。

## 已落地

- D050：冻结 a08-dev-v1 的 20 条开发样本，10 条正向检索、10 条权限/状态/记忆边界；金标绑定真实 chunk_id、段落范围、版本和角色/接收者交集。标签由 AI 核对，尚不是学习者独立成果。
- D051：宏平均 Recall@3 与截断 MRR@3；覆盖手算、多相关、重复排名、空结果、非法 k、无正向样本、服务失败记零、未运行标 incomplete。候选、最终 top3、完整片段装包分别记录。
- D052：每路内部 top5，单路去重，RRF 常数 60，稳定 chunk_id 并列排序；hybrid_fake/hybrid_real 分开。权限先于评分。显式允许时向量故障才降级关键词。
- D053：重排默认 off，Fake 与显式 Real 适配器；最多 10 个授权片段、每块 400 字符、总输入 8000 字符。严格完整排列，未知/重复/遗漏 ID 拒绝；单次最多 5 秒、一次尝试；失败且仍有预算时回退本次原榜。
- D054：三组确定性序列及故障、同轮 L12 受众隔离、隐藏文档镜像、装包裁剪后拒绝旧引用；重放不产生新调用或事件。
- D055：显式 A08 教学 profile：20 个新互动轮、故事最多 80 次、演示批次最多 180 次；每轮仍是 8 次/30 秒/8000 字符。用户显式给更低请求上限时继续从低。Fake 主路径 20 轮，三个角色参与，另局验证相反分支。
- D056：轮边界 JSON 存档与跨进程续玩。世界事件、角色历史、幂等回执、导演、结束状态、请求分项/轮数/每轮配置/批次额度与配置身份一同恢复。

默认 lore 仍 off、重排 off。没有引入框架、数据库、Web/API 层或新剧情。

## 最终验证

全部命令在不含 .env 的冻结源码副本中运行；源码压缩包、原始字节哈希、完整命令、Python 环境、时间和退出码见
[冻结证据清单](a08_verification/20260917T135845603877/manifest.json)。
测试后核对工作区对应源码哈希未变，LF/CRLF 按原始字节计。

| 项目 | 实际结果 |
|---|---|
| A08 专项 | 33 项通过 |
| 全量回归 | 311 项通过，已包含专项，不能相加 |
| 开发集 | 20 个不同案例；10 条正向 × 4 路评测；10 条确定性边界检查通过 |
| 演示 | A08 跨进程 20 轮与两种结局；A07、A06、A05 旧演示通过 |
| 本任务真实服务调用 | 0 |
| Git 提交/推送 | 未执行 |

第一轮发现 diagnostics.cache_hits 使旧测试的同查询结果比较不稳定，已改为默认仅进入 trace，
评测器显式请求诊断；原测试未放宽。早期失败输出保留，最终冻结回归已覆盖这个兼容问题。

## 检索对照

[完整逐例报告](a08_retrieval_report.json)，固定 k=3、10 条正向分母、同一设定和金标：

| 后端 | Recall@3 | MRR@3 |
|---|---:|---:|
| keyword | 1.00 | 1.00 |
| vector_fake | 0.55 | 0.60 |
| hybrid_fake / RRF | 1.00 | 1.00 |
| hybrid_fake + Fake rerank | 1.00 | 1.00 |

未观察到混合/重排优于关键词。Fake 向量的 01、03、04、10 丢失金标，06 漏掉一个相关片段。
这仅说明当前哈希词面向量、阈值和开发样本下的结果，不能推广到真实语义模型。
报告保留逐例差值、各路名次、融合分、缓存元信息、原始耗时/中位数/最大值及请求分项，不报生产 p95。

装包指标在固定最小生成输入夹具中实际调用 pack_lore；不是完整真实聊天的指标。
20 轮演示另保留实际 Fake 生成请求的交付引用。11—20 检查程序边界；
真实无支持断言数、正确拒答/澄清数没有用预写 Fake 台词代评分。

## 状态与欠项

| 任务 | 离线工程 | 真实效果 | 本人掌握 |
|---|---|---|---|
| D050 标注 | 已验证 | 开发集，不是保留集 | 本人核查待完成 |
| D051 指标 | 已验证 | Real 同集报告未运行 | 本人手算待完成 |
| D052 混合 | 已验证 | 真实 RRF 同集对照待完成 | 独立复述待完成 |
| D053 重排 | 已验证接口与反例 | 未配置/未运行真实重排；不能写成无收益 | 独立注入变式待完成 |
| D054 多轮 | 已验证确定性序列 | 真实模型行为待观察 | 本人解释待完成 |
| D055 试玩 | Fake 20 轮通过 | 真实主局与另一分支待完成 | 本人自由试玩待完成 |
| D056 续玩 | 跨进程恢复/重放通过 | 真实第 10→11 轮待完成 | 本人操作验收待完成 |

并行任务已提供 [A07 阿里云真实接入证据](a07_real_results.md)：Embedding 接通与三类样例，
同时保留一句无支持措辞的质量缺口。本轮只引用该任务的报告，不将它当作 A08 真实成绩；
A05/A06 真实与学习欠项、A07 原预测/SQL/源码复述、旧清单差异继续按原记录追踪。
固定 LangChain 源码本轮读取失败；[源码笔记](a08_source_notes.md) 明确区分已有计划说明和本轮已读源码。

下一步按原计划：学习者先补原预测；显式冻结真实评测阈值/供应商/费用与请求预算后运行 Real 对照；
完成真实 20 轮自由互动和存档续玩，再做本人独立变式。未满足前不写“整课通过”。
本轮 AI 完成实现、标签核对、自动检查和参考笔记，没有代填本人答案。

## 可复现命令

```powershell
.\.venv\Scripts\python.exe -X utf8 -m scripts.a08_verify
.\.venv\Scripts\python.exe -X utf8 -m scripts.a08_eval
.\.venv\Scripts\python.exe -X utf8 -m scripts.a08_demo
```

以上默认完全离线；verify 每次写新时间戳目录，不覆盖旧课程证据。
[试玩记录](a08_playthrough.md) · [存档操作与限制](a08_save_resume.md) ·
[实施契约](a08_contract.md) · [本人预测](a08_prediction.md) · [用户原计划](a08_plan.md)。
