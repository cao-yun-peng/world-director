# D002 本人练习与口头验收

这些是需要本人补交的学习证据，不因 AI 已完成项目代码而自动通过。

## 闭卷复写

不看 D001 实现，自己在空白文件中写 `count_calls(calls)`，不用 `Counter`。
完成后用以下用例验收，并提交自己写的代码和运行输出：

```python
assert count_calls([]) == {}
assert count_calls(["talk", "talk", "talk"]) == {"talk": 3}
assert count_calls(["inspect", "talk", "inspect"]) == {"inspect": 2, "talk": 1}
```

本人实现：待补。
本人输出：待补。

说明为什么遍历 n 次调用的时间是 O(n)，而不同名称数为 k 时空间是 O(k)。

## 独立解释

用自己的话写100—200字：本次是否出现策略差异？哪些具体输出支持你的判断？哪些输出违反事实约束？

本人解释：待补。

## 两道检查题

1. 再加一个“希望玩家帮忙”的目标，需要修改 `RealModelAdapter` 吗？为什么？
2. 两版都回答“请先说明来意”，能立即断言目标无效吗？会检查什么证据？

本人回答：待补。

<details>
<summary>完成独立回答后再展开：AI提供的核对要点</summary>

- 统计时每个名称处理一次；字典查询与更新平均 O(1)，因此时间 O(n)。字典保存 k 个不同名称及计数，空间 O(k)。
- 新增目标只需扩展 `GOALS`，仍通过 `build_prompt()` 生成 system 消息，适配器继续接收相同消息格式，无需理解目标。
- 单条相同回复不能证明目标无效。先检查实际 goal_id、goal、prompt_version、system/user 分区、模型及参数是否一致，再看追问在当前目标下的作用。若请求确实只改变目标但行为相同，应如实记录本轮未见预期差异；四次调用的预算用尽后不追加抽样。

</details>
