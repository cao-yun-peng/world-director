# A07 阿里云真实接入与样例复核

2026-09-17，revision 2，任务 A07-REAL-20260917。基线为用户已提交的 `eca50dd`（A07）。用户明确选择阿里云并授权复用现有密钥。

**真实 Embedding 已接通；三类回答样例已运行，无依据路径修复后可正常结束。真实生成仍有一处无依据措辞，质量 Gate 保留待改进；学习者独立验收未完成。**

## 当前配置与体验

本机 `.env` 已追加：Embedding 模型 `text-embedding-v4`，1024 维，阿里云现有兼容端点。`EMBEDDING_API_KEY=${LLM_API_KEY}` 通过变量引用复用现有密钥，没有把密钥复制进代码、文档或日志。聊天模型沿用 `qwen3.7-plus`。

在项目根目录运行：

```powershell
.\.venv\Scripts\python.exe -X utf8 -m app.main --engine scene --lore-mode vector_real --build-lore-index --allow-lore-upload --lore-min-score 0.5 --max-model-requests 24
```

可依次问“值班室用于什么？”、“地下通道密码是什么？”、“沈岚旧目录规则的别名是什么？”。CLI 根据通过校验的 lore_refs 显示“资料依据”，不用依赖模型在台词中重复编号。`/retry` 不新增请求，`/new` 不重置进程额度，`/exit` 退出。启动构建会消耗少量真实请求；阈值 0.5 是当前小样本试验值，尚未系统校准。

适配依据：[阿里云文本向量官方接口](https://help.aliyun.com/zh/model-studio/text-embedding-synchronous-api)。供应商模型别名没有固定版本保证，记录 provider-unpinned。

## 实际结果

| 查询/范围 | 真实检索结果 |
|---|---|
| 值班室用于什么／林砚给玩家 | 第一名 L02，余弦约 0.840 |
| 来客通常在哪里登记交接事情／林砚给玩家 | 同义问法第一名 L02，约 0.579 |
| 交接表栏目顺序／林砚给玩家 | 第一名 L12，约 0.763 |
| 地下通道密码／林砚给玩家 | NO_USABLE_EVIDENCE，空结果 |
| 沈岚目录别名／林砚给玩家 | NO_USABLE_EVIDENCE，空结果 |
| 沈岚目录别名／沈岚自身，无生成 | 第一名 L10，约 0.836；不送入给玩家的生成请求 |

有据回答准确复述值班室用途，实际 lore_refs 指向 L02。无权回答没有输出私密别名，也没有透露隐藏命中的存在。所有已运行模型请求均检查过：没有 L07/L10、私密代号、私密标题，最大输入 7945 Unicode 字符，低于 8000；信封始终归林砚，没有转移事实。

首轮无依据回答未完成：模型把本轮尚未提交的玩家私语候选写入 source_refs，程序正确拒绝，模型重复错误最终 STEP_LIMIT。保留这一失败，不能用进程 exit 0 冒充所有业务路径通过。

修复只增加具体错误反馈，明确指出 source_refs 不能引用私语候选，缺少事件依据时应填空列表；同时提醒空检索后结束答复，不转向无关物品查询。没有放宽 ACL、事件来源或 lore_refs 校验。运行协议升级为 a07-lore-v2，设定仍为 a07-lore-v1。

针对性复测得到 completed，回答承认不知道密码，lore_refs 为空。但附加的“这类信息通常不会记录在灯塔的日常管理资料中”没有资料支持，仍是措辞质量缺口。本轮没有编造密码，也没有将这句话写成世界事实；不能据此宣称已消除幻觉。停止追加真实调用，将该样例留给后续提示与评测改进。

## 预算和证据

首轮探针 18 次外部尝试；独立复测限制为剩余 6 次，实际 5 次；**本轮合计 23 次**（13 次 Embedding、10 次聊天）。没有因失败重新放大预算。首轮两条正常生成使用修复前协议，复测无依据案例使用修复后协议；没有声称三个案例全部在修复后重跑。

- [逐条复核结果](a07_real_review.json)：查询、命中、普通回复、真实请求 ID、输入隔离检查和已知缺口。复核者是 AI，用户人工审阅仍为 pending。
- [新证据清单](a07_real_evidence_manifest.json)：完整命令、退出码、当前代码和原始证据 SHA-256。
- [首轮原始证据](../runs/a07_real_io/first-probe-20260917T132126.json)：保留 STEP_LIMIT；本地忽略，不自动提交。
- [复测原始证据](../runs/a07_real_io/retest-20260917T132730.json)：保留真实措辞；本地忽略。
- [修复专项](a07_real_fix_tests.txt)：新增 2 项通过。
- [修复后全量](a07_real_fix_all_tests.txt)：278 项通过，exit 0。

仅执行真实探针、受限复测和离线测试；未安装依赖、未启动数据库、未修改世界裁定、未创建新 Git 提交。旧离线报告与失败日志保留。A05/A06 独立真实验证、个人预测、SQL 手算和源码复述的欠项没有因此自动关闭。


## 并行工作区的证据边界

收尾发现另一项 A08 任务正在修改检索、预算和场景模块；未覆盖或回退这些改动。上面的 278 项全量结论对应当次测试进程，测试后工作区继续变化，不能将后来采集的全部文件哈希当作该进程受测版本证明。

为避免继续追逐共享目录的变化，另冻结不含 .env 的临时快照运行 A07 专项，记录见 [冻结快照清单](a07_concurrent_snapshot.json) 与 [专项输出](a07_concurrent_snapshot_tests.txt)。后续 A08 修改由其任务单独验收。真实服务证据仍对应已保存的实际请求与原始回复。


冻结快照实际结果：50 项 A07 测试中 49 项通过、1 项失败。失败位于 `test_fixed_queries_both_backends`：A08 新增 diagnostics.embedding.cache_hits，第二次查询从 0 变为 1，原测试比较整个结果对象而非只比较命中与排名。固定来源命中断言通过；这是新增诊断字段与原确定性断言的兼容问题，留给正在进行的 A08 任务合并处理。本轮未修改 A08 实现或该旧测试，未把这次快照报告为全绿。
