# 项目开发约束

本项目当前处于任务的第二天。后续任务都应在本项目中完成，以简洁、可扩展、适合人类阅读和学习的代码为目标。

## 编码原则

1. **简洁易读**：优先使用直白的命名、清晰的控制流程和职责单一的函数。避免炫技写法、过深嵌套和不必要的间接调用。
2. **适度可扩展**：通过合理的模块划分和明确的接口隔离变化。围绕已明确的需求设计，出现实际复用或扩展需求时再抽象，不为假想需求搭建框架。
3. **不过度防御**：在外部输入、文件读写和外部服务等边界保留必要校验与错误处理。内部代码遵循明确的约定，避免重复校验、无意义的兜底和层层捕获异常；不要静默吞掉错误。
4. **适合作为学习范本**：让读者能顺着代码理解任务如何完成。注释重点解释设计原因和必要的背景，避免复述代码；必要时提供小而清楚的使用示例。
5. **符合当前阶段**：优先完成当前任务的最小完整实现。新增依赖、配置项、抽象层或通用机制前，确认它能解决当前的具体问题。

## 完成任务的要求

- 遵循现有项目结构和命名习惯，控制修改范围。
- 对关键行为进行与改动规模相称的验证，避免为简单改动堆砌测试。
- 交付时简要说明改了什么、为什么这样设计，以及如何验证。
- 自查：人类是否容易读懂？扩展时是否有清晰的修改位置？是否存在可以删除的抽象、重复校验或兜底逻辑？
